const mvS = new Swiper('.mv_in', {
    loop: true,
    autoplay:{delay:2500},
    scrollbar: {el: '.mv_in .swiper-scrollbar'},
});
const bestS = new Swiper('.best_s', {
    
    scrollbar: {el: '.mv_in .swiper-scrollbar'},
});